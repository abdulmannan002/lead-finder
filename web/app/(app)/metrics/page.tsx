import { PlaceholderPage } from '@/components/placeholder-page';

export default function MetricsPage() {
  return (
    <PlaceholderPage
      title="Metrics"
      description="Daily rollups: sends, replies, bounces, reply rate and funnel."
      milestone="M4"
    />
  );
}
